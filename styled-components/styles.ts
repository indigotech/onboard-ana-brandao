import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  fab: {
    position: 'absolute',
    margin: 16,
    right: 10,
    bottom: 10,
  },

  radioButton: {
    color: '#777777',
    textAlign: 'center',
  },
});
